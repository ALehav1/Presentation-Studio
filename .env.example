# PresentationStudio Environment Variables

# REQUIRED: OpenAI API Key for AI features
# Get your key from: https://platform.openai.com/api-keys
# SECURITY: This should ONLY be set on your backend/server
# NEVER expose this in client-side code or localStorage
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom rate limiting (requests per minute)
OPENAI_RATE_LIMIT=60

# Optional: Maximum request size in bytes  
MAX_REQUEST_SIZE=50000

# Development settings
NODE_ENV=development

# Production: Set this on Vercel/Netlify deployment
# Vercel: Settings → Environment Variables → Add OPENAI_API_KEY
# Netlify: Site Settings → Environment Variables → Add OPENAI_API_KEY

# API Endpoints (automatically configured):
# - /api/ai/read-slide (main slide analysis endpoint)
# - /api/ai/read-slide/health (health check endpoint)
# - /api/openai-proxy (general OpenAI proxy endpoint)
